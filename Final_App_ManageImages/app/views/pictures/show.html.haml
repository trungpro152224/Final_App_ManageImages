
%h1 DETAIL PICTURE
<br>
= image_tag @picture.image.url(:large) 
%h2.title=@picture.title 
%p.description= @picture.description
	
<br>
	
.buttons
	= link_to (t'.all_picture'), pictures_path
	= link_to (t'.edit_picture'), edit_picture_path(@picture)
	= link_to (t'.delete_picture'), picture_path(@picture), method: :delete, data: {confirm: "Are you sure deleted?"}
