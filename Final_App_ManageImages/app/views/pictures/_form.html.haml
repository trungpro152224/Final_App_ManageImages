= simple_form_for @picture, html: { multipart: true } do |f| 
  / = f.file :Choose file 
  = f.label :image
  = f.file_field :image
  = f.input :title 
  = f.input :description 
  = f.button :submit, class: "button"
