.listing-album 
  %h1 LISTING ALBUMS

  %table.table.table-hover.table-responsive.show-empty-cells
    %thead

      %tr
        %th Name
        %th Description
        %th Public
        %th
        %th
        %th
        %thead

    %tbody
      - @albums.each do |album|
        %tr
          %td= album.name
          %td= album.description
          %td= album.is_public
          %td= link_to 'Show', album, class: "btn btn-success"
          %td= link_to 'Edit', edit_album_path(album), class: "btn btn-info"
          %td= link_to 'Destroy', album, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger"

  %br

  = link_to 'New Album', new_album_path, class: "btn btn-info"
  = link_to 'Cancel', pictures_path, class: "btn btn-info"
